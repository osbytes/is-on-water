dist
node_modules
.tap

# Cached upstream sources (hundreds of MB each)
data/_osm/
data/_hydrolakes/

# Intermediate GDAL output and downloaded (non-bundled) layers
data/_*.tmp.fgb
data/_layer-cache/
data/layers/*.fgb
